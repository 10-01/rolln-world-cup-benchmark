{
  "name": "world-cup-bench",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run prepare-data && next build",
    "start": "next start",
    "prepare-data": "node scripts/prepare-data.mjs",
    "fetch-results": "node scripts/fetch-results.mjs && node scripts/prepare-data.mjs",
    "check": "npm run prepare-data && next build"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "next": "^15.0.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "redis": "^6.0.0"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/react": "^19.0.1",
    "@types/react-dom": "^19.0.1",
    "typescript": "^5.7.2"
  }
}
